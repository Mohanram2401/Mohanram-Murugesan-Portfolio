rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Anyone may read published content (portfolio renders for visitors).
    allow read: if true;

    // Only signed-in admin users (Firebase Auth email/password) may write.
    allow create, update, delete: if request.auth != null;
  }
}
